#!/bin/bash
# Basic command to run the script for MNIST dataset
# Arguments are in order:
# beta, lr, sigma, batch_size, P, seed, feature_type

python Conv_relus_MNIST.py 0.01 0.00031 5.0 1024 100 42 1
